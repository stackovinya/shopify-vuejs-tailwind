

<div class="flex items-center bg-gray-100">
    <div class="container max-w-5xl grid grid-cols-12 gap-3 items-center px-6 py-10">
        <div class="col-span-12 flex flex-col justify-center gap-3">
            <h1 class="text-2xl md:text-3xl text-primary font-bold uppercase">{{ article.title }}</h1>
            <p class="">{{ article.excerpt }}</p>
        </div> 
    </div>
</div>


<div class="container max-w-5xl grid grid-cols-12 gap-3 px-6 py-10">
    <div class="col-span-12 block shopify_richText">
        {{ article.content }}
    </div>
</div>