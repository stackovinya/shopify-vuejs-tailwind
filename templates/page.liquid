<div class="">
   <div class="container max-w-5xl grid grid-cols-12 gap-3 my-4 px-5 py-10">
      <div class="col-span-12 md:col-span-6 flex flex-col justify-center px-4">
         <span class="text-primary uppercase text-sm mb-3">{{ page.metafields.custom.page_type }}</span>
         <h1 class="text-3xl md:text-5xl font-bold mb-3">{{ page.title }}</h1>
         <p class="text-lg mb-3">
            In today's digital age, influencer marketing has become a cornerstone of successful business strategies. By collaborating with right influencers can boost your brand awareness efforts.
         </p>
         <div class="my-4 self-start">
            {% render "buttons", button: "simple", text: "Talk to us", href: "/pages/contact" %}
         </div>
      </div>
      <div class="col-span-12 md:col-span-6 flex flex-col px-1 md:px-4">
         <img class="w-full" src="{{ page.metafields.custom.banner_image | file_url }}" />
      </div>
   </div>
</div>

<div class="container max-w-5xl shopify_richText px-5 my-5">
   {{ page.content }}
</div>

{% section "slide-show" %}

<div class="container max-w-5xl px-5 my-3">
   {% render "talk-to-us-block" %}
</div>



